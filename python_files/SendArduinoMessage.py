"""
SendArduinoMessage.py

if monitoring needs to be done on my laptop,
then an Arduino would be more suited
to doing the mechanical half of the work.

This module would handle sending input to the arduino (hopefully by usb) to trigger it.
"""